{
  "$schema": "../gen/schemas/desktop-capability.json",
  "identifier": "desktop",
  "description": "Permissions that only apply to the desktop platforms — the tauri-plugin-window-state crate is gated out of the Android build via cfg(not(target_os = \"android\")), and referencing window-state:default in the cross-platform default capability would fail Android's capability validation.",
  "windows": ["*"],
  "platforms": ["linux", "macOS", "windows"],
  "permissions": [
    "window-state:default"
  ]
}
